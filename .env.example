# Git Analytics Environment Configuration
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The .env file is gitignored and will not be committed.

# GitHub Personal Access Token (PAT)
# Required for API-based extraction (extract-api.js)
# Create at: https://github.com/settings/tokens/new
# Scopes needed: 'repo' (private repos) or 'public_repo' (public only)
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: GitHub Enterprise Server URL (leave empty for github.com)
# GH_HOST=github.mycompany.com
